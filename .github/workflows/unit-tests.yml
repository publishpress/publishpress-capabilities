name: Unit and Integration Tests
permissions:
  contents: read
on:
  pull_request:
    branches:
      - 'master'
      - 'development'
      - 'release/*'
      - 'release-*'

  workflow_call:

  workflow_dispatch:

jobs:
  check:
    uses: publishpress/github-workflows/.github/workflows/unit-tests.yml@v3
